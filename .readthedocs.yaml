version: 2
sphinx:
  configuration: docs/source/conf.py
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install uv
    install:
      - uv sync --group docs
